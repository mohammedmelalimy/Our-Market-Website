{
  "dependencies": {
    "json-server": "^0.17.4",
    "json-server-auth": "^2.1.0"
  },
"scripts": {
  "start": "npx json-server-auth --port $PORT -w db.json",
  "dev": "npx json-server-auth --port 3000 -w db.json"
}
}